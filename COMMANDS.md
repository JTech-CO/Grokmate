# 명령어 (Commands)

## 호출 방법

- **웨이크워드**: 메시지 맨 앞에 `그록아`(한국어) 또는 `Hey Grok`(영어)
- **멘션**: `@Grokmate ...`
- **DM**: 봇에게 다이렉트 메시지
- **전용 AI 채널**: 관리자가 `/config ai-channel add` 로 지정한 채널에서는 멘션·웨이크워드 없이 그냥 말하면 응답
- **자연어 링크 읽기**: 링크에 "읽어 / 요약"을 덧붙이면 페이지를 가져와 요약
- **자연어 서버 관리(권한 필요)**: "슬로우모드 10초로", "역할 부여" 등
- **커스텀 명령어·자동응답**: 서버가 등록한 `!트리거` 를 치거나, 등록된 키워드가 메시지에 들어가면 고정 문구로 응답(AI 미경유)

응답 언어는 웨이크워드를 따릅니다(`그록아`→한국어, `Hey Grok`→영어, 멘션·DM→한국어).

## 누구나 쓰는 슬래시 명령

| 명령 | 설명 |
|---|---|
| `/help` | 명령어·기능 안내(본인만 표시). 하단 버튼으로 일반 사용자 / 관리자 목록 전환 |
| `/reset` | 나와 나눈 대화 맥락(기억) 초기화. 최근 대화창과 **저장된 요약을 함께** 지웁니다 |
| `/ping` | 봇 응답·지연 확인 |
| `/debug` | 진단: 현재 시각(KST)·핑·RAM·디스크·CPU 정보·네트워크 속도(실측)·채널 수 (동일인 1분 쿨타임) |
| `/level [user]` | 레벨·XP 확인(대상 생략 시 본인) |
| `/leaderboard` | 서버 레벨 순위(상위 10) |
| `/poll <질문> <선택지…>` | 투표/설문(버튼 집계, 다중·비밀·자동마감 옵션) |
| `/verify-age` | 만 19세 이상 자가 인증(성인 채널 이용용, 본인만 표시). **각자 한 번씩** 필요하며 디스코드 자체 연령 확인과는 별개 |
| `/usage` | 내 토큰 사용량·요금제 확인 및 업그레이드(Basic/Pro/Max) |
| `/repo <검색어>` | GitHub 공개 레포 검색(별점순 상위). `/gh-repo` 로도 부를 수 있습니다(같은 명령) |
| `/gh-user <유저명>` | GitHub 사용자 프로필·레포 목록 |
| `/gh-file <owner/repo> [경로]` | 레포 파일 내용·디렉터리 개요 |
| `/gl-repo <검색어>` | GitLab 공개 프로젝트 검색(별점순). 검색어는 **3글자 이상**이어야 결과가 나옵니다 |
| `/gl-user <유저명\|그룹>` | GitLab 유저·그룹의 프로젝트 목록(그룹은 하위 그룹 포함) |
| `/gl-file <group/project> [경로] [브랜치]` | GitLab 파일 내용·디렉터리 개요. 중첩 그룹·웹 URL 붙여넣기 가능 |
| `/hf-repo <검색어> [종류] [정렬]` | 허깅페이스 검색. 종류 = 모델·데이터셋·스페이스, 정렬 = 다운로드·좋아요·최근 수정·최근 생성. 레포 **이름** 기준이라 여러 낱말을 넣으면 결과가 줄어듭니다 — 0건이면 낱말을 줄여 다시 찾고 그 사실을 알려줍니다 |
| `/hf-user <유저명\|조직>` | 허깅페이스 유저·조직의 레포 목록(다운로드순). 유저인지 조직인지 함께 표시 |
| `/hf-file <owner/name> [종류] [경로] [리비전]` | 허깅페이스 레포의 파일 내용·디렉터리 개요. `gpt2` 같은 단일 이름·웹 URL 도 됩니다 |
| `/fetch <url> [ask]` | 웹 페이지를 읽어 요약(출처 포함) |
| `/hn <검색어> [sort] [period] [count]` | Hacker News 검색. 정렬 = 인기순·최신순·댓글순, 기간 = 24시간~전체. **말로 시켜도** 됩니다: "해커뉴스에서 Machine Learning 다룬 최신 기사 3개만 찾아줘" 처럼 하면 정렬·기간·개수까지 알아듣습니다. AI 미경유라 토큰 0 |
| `/translate <이미지> [대상언어]` | 이미지 속 글자를 OCR 추출 후 번역 |
| `/voice <name\|limit\|lock\|unlock>` | 내가 만든 임시 음성채널 제어(방장) |
| `/tts <내용>` | 내가 있는 음성 채널에서 문장을 읽어줌 (최대 300자, 링크·멘션·코드는 읽지 않음) |
| `/tts-stop` | 낭독을 멈추고 음성 채널에서 나감 |
| `/tts-settings` | 내 목소리·재생 속도 설정(본인만 표시): 목소리는 유지, 속도는 음성방을 나가면 초기화 |

## 음성 낭독 (TTS)

봇이 음성 채널에서 한국어로 문장을 읽어줍니다. 음성은 xAI TTS로 생성합니다.

- **자동 입·퇴장**: 사용자가 음성 채널에 들어오면 봇도 따라 들어가고, 사람이 한 명도 남지 않으면 나갑니다.
- **`/tts <내용>`**: 지정한 문장을 낭독합니다(호출자가 그 음성 채널에 있어야 함).
- **채팅 자동 낭독**: 관리자가 `/config tts-channel set` 으로 지정한 텍스트 채널의 메시지를, 봇이 음성 채널에 있을 때 자동으로 읽습니다.
- **개인 설정(`/tts-settings`)**: 목소리 16종(여성 4·남성 12) 중 선택. **목소리는 다시 바꾸거나 초기화할 때까지 유지**되고, **재생 속도(0.8~1.5배)는 음성 채널을 나가면 1.0으로 초기화**됩니다. 초기화 버튼으로 둘 다 되돌립니다.
- **읽지 않는 것**: 링크·멘션(`@everyone` 포함)·코드블록·커스텀 이모지는 낭독에서 제외됩니다(봇 목소리로 타인을 호명하거나 링크를 읽는 것을 막기 위함). 길이 상한 300자, 유저별 쿨타임, 서버 일일 문자 쿼터가 적용됩니다.
- **AI 답변은 음성으로 읽지 않습니다.** 음성 채널에는 텍스트 채널의 '연령 제한' 게이트에 대응하는 개념이 없어, 성인 모드 응답이 음성으로 새는 경로를 원천 차단하기 위한 설계입니다.

## 커스텀 명령어 · 자동응답

서버가 직접 만든 고정 문구 응답입니다. AI를 거치지 않으므로 토큰을 쓰지 않고 즉시 응답합니다.

- **두 종류**: `!트리거` 로 부르는 **커스텀 명령어**, 메시지에 키워드가 들어가면 자동으로 답하는 **자동응답**.
- **등록은 스탭 이상(`/custom add`)**: 일반 멤버는 만들 수 없습니다. 서버 소유자·관리자 권한 보유자·`/config staff` 로 지정된 스탭 역할이 대상입니다.
- **관리(`/custom list`)**: 목록에서 바로 삭제·수정합니다. 관리자 이상은 서버의 모든 항목을, 스탭은 **자신이 만든 항목만** 다룰 수 있습니다.
- **도배 방지 한도**: 계정당 **하루 5개**(요금제 무관, 기존 항목을 지우면 그만큼 다시 등록 가능), 서버당 50개, 트리거 200자·응답 1,000자, 유저별 쿨타임.
- **권한 대여 차단**: 응답 문구에 넣은 `@everyone`·`@here`·역할 핑은 봇 출력에서 항상 차단됩니다.

## 성인(NSFW) 모드 여는 법

네 관문을 **모두** 통과해야 열립니다. 하나라도 빠지면 평소처럼 일반 모드로 답합니다.

1. **채널**: 디스코드 채널 편집에서 `연령 제한 채널` 켜기.
2. **서버**: 관리자가 `/nsfw-policy` 로 서버 성인 모드 켜기.
3. **사용자**: 대화할 사람이 **각자 한 번** `/verify-age` 로 인증.
4. **대화 내용**: 그 메시지가 실제로 성적인 내용일 때.

> **3번을 가장 많이 놓칩니다.** 디스코드가 연령제한 채널 입장 때 하는 확인과 Grokmate 인증은 **별개**입니다. 그 결과는 봇에게 전달되지 않으므로 각자 눌러야 합니다. 성인 채널에 `/verify-age` 안내를 공지로 고정해 두면 편합니다.

봇을 서버에 추가하면 환영 메시지에, `/nsfw-policy` 를 켜면 그 결과에 같은 안내가 함께 나갑니다. 미성년 관련 내용은 게이트와 무관하게 항상 차단됩니다.

## 관리자·스탭 전용

> 권한이 없으면 명령 목록에 표시되지 않습니다.

**모더레이션 (ModerateMembers 등)**

| 명령 | 설명 |
|---|---|
| `/timeout <user> <분> [사유]` | 타임아웃 |
| `/untimeout <user> [사유]` | 타임아웃 조기 해제 |
| `/kick <user> [사유]` | 킥 |
| `/ban <user> [사유]` | 밴 (확인 버튼) |
| `/unban <user_id> [사유]` | 차단 해제(Discord ID로 지정) |
| `/purge <개수>` | 메시지 청소 (확인 버튼) |
| `/warn <user> <사유>` | 경고 부여(누적 시 자동 타임아웃) |
| `/warnings <user>` | 경고 이력 조회 |
| `/pardon <user> [번호\|count]` | 경고 사면: 특정 번호·최근 N건(개수 줄이기)·전체 |

**채널 관리 (ManageChannels)**

| 명령 | 설명 |
|---|---|
| `/slowmode <초>` | 채널 슬로우모드(0~21600초) |
| `/create-channel <이름> [종류]` | 채널 생성(텍스트/음성) |
| `/delete-channel <채널>` | 채널 삭제 (확인 버튼) |
| `/lock` · `/unlock` | 현재 채널 잠금/해제 |

**서버·역할 설정 (ManageGuild / ManageRoles)**

| 명령 | 설명 |
|---|---|
| `/config status` | **서버 설정 패널**: 버튼·셀렉트로 바로 변경. 섹션 4종(기본·채널·역할·TTS), 채널·역할은 한 번에 교체 |
| `/config <reset\|honorific\|manage-via-nl\|automod\|ai-channel\|log-channel\|tts-channel\|tts-voice\|staff\|admin>` | 같은 설정을 슬래시로 직접 지정(패널을 안 거치고 싶을 때) |
| `/welcome <set\|goodbye\|off\|status\|test>` | 가입/퇴장 환영·작별 메시지 |
| `/autorole <set\|off\|status>` | 가입 시 자동 역할 |
| `/reactionrole <add\|remove\|list>` | 메시지 반응 → 역할 자동 부여 |
| `/voice-hub <set\|off\|status>` | 임시 음성채널 허브(Join-to-Create) |
| `/nsfw-policy <on/off>` | 서버 성인 모드 활성/비활성 (확인 버튼). 켜면 결과에 **남은 게이트 안내**가 함께 표시 |
| `/custom <add\|list>` | 커스텀 명령어(`!트리거` 호출)·자동응답(키워드 감지) **(스탭 이상)**. `list` 는 목록에서 바로 삭제·수정. 계정당 하루 5개, 서버당 50개, 트리거 200자·응답 1,000자 |

밴·킥·타임아웃·청소는 채팅(자연어)으로 실행하지 않고 **슬래시 명령 + 확인 버튼**으로만 실행됩니다. 슬로우모드·역할 부여/회수·채널 생성 등은 권한자가 자연어로도 지시할 수 있습니다.

## 봇 운영자 전용

> 봇을 직접 운영하는 사람(설정된 운영자 ID)만 사용할 수 있으며, 서버 관리자 권한만으로는 실행되지 않습니다.

| 명령 | 설명 |
|---|---|
| `/maintenance <on/off> [message]` | 점검 모드: 켜면 점검이 끝날 때까지 모든 대화·명령에 "점검 중입니다" 안내(재시작해도 유지) |
| `/cost` | 토큰 사용량 집계(오늘·최근 7일) |
| `/health` | 봇 상태: 가동률(24시간·7일·30일)·재시작 횟수·동시성·OCR 대기·업타임·메모리·점검 모드 |
| `/guilds` | 소속 서버 **운영 대시보드**: 서버명·멤버 수·채널 목록(연령제한 여부 표시)·설정 요약·마지막 활동에 더해, 서버를 골라 **일시정지/재개 · 런타임 초기화 · 대화 맥락 초기화 · 성인 모드 끄기 · 일일 토큰 상한**을 조작. 모든 조치는 **그 서버에만** 적용되고 다른 서버는 영향을 받지 않으며, 전부 감사 로그에 남습니다 |
| `/audit` | 최근 권한(관리 액션) 감사 로그 |
| `/grant-tier`·`/grant-list` | 유료 티어 수동 부여·활성 구독 조회 |

모든 관리 액션은 **요청자 권한으로 단일 인가 게이트를 거치고 전수 감사 로그**에 기록됩니다. `/config log-channel` 지정 시 자동 모더레이션 삭제·경고·사면이 로그 채널에도 임베드로 기록됩니다. 자동 역할·반응 역할은 관리 권한이 있는 역할을 지급 대상으로 설정할 수 없습니다(권한 상승 차단). 봇에게 다른 유저·@everyone 멘션을 시키는 요청은 스탭 이상만 가능하며, 봇 출력에서 @everyone/@here/역할 핑은 항상 차단됩니다(봇 권한 대여 방지).

# 변경 이력 (Changelog)

Grokmate에 실제로 반영된 변경을 이용자 관점에서 정리합니다.

> 웹에서 보기: [changelog.html](<https://jtech-co.github.io/Grokmate/changelog.html>) (같은 내용)

Grokmate는 **호스팅형 봇**이라 이용자가 버전을 골라 설치하지 않습니다. 그래서 판올림 번호 대신 **반영된 날짜**로 적습니다. 서버에 봇이 들어와 있다면 항상 최신 버전입니다.

내부 리팩터링·테스트·문서 정리처럼 사용감이 달라지지 않는 변경은 생략했습니다. 전체 기록은 커밋 로그에 있습니다.

**표기**: `추가` 새 기능 · `변경` 동작·정책이 달라짐 · `수정` 버그 고침 · `보안` 권한·안전 관련

---

## 2026-08-05

- `추가` **커스텀 명령어·자동응답** (`/custom`). 서버가 직접 만든 `!트리거` 명령과 키워드 자동응답에 고정 문구로 답합니다. AI를 거치지 않아 토큰을 쓰지 않습니다.
  - 등록은 **스탭 이상**만. `/custom list` 는 목록에서 바로 삭제·수정하는 패널이며, 관리자 이상은 서버 전체를, 스탭은 자기가 만든 것만 다룹니다.
  - 도배 방지: 계정당 하루 5개(요금제 무관, 지우면 회복), 서버당 50개, 트리거 200자·응답 1,000자.
- `추가` `/help` 를 **일반 사용자 / 관리자 두 뷰**로 나누고 버튼으로 전환하게 했습니다. 명령이 늘어 한 화면에 다 담기 어려워졌기 때문입니다.
- `수정` 시스템 프롬프트에 남아 있던 옛 문장 때문에 봇이 "난 실시간 검색을 못 한다"고 잘못 답하던 문제. 실제로는 검색 모드로 자동 전환됩니다.
- `변경` 한국어 안내 문구에 섞여 있던 영문식 대시(—)를 쌍점 등으로 바꿨습니다.

## 2026-08-04

- `추가` **음성 낭독(TTS)**. 음성 채널에서 문장을 읽어줍니다.
  - `/tts` 로 문장 낭독, `/tts-stop` 으로 중지·퇴장.
  - 관리자가 `/config tts-channel` 로 지정한 텍스트 채널의 채팅을 자동으로 읽습니다.
  - 사용자가 음성 채널에 들어오면 봇도 따라 들어가고, 사람이 다 나가면 나갑니다.
  - `/tts-settings`: 목소리 16종 중 선택(유지됨), 재생 속도 0.8~1.5배(음성 채널을 나가면 1.0으로 초기화).
  - 링크·멘션·코드블록은 읽지 않으며, **AI 답변은 음성으로 내보내지 않습니다**(음성 채널에는 텍스트 채널 같은 연령 게이트가 없기 때문).

## 2026-07-23

- `수정` '밴드'·'청소년'·'킥보드'처럼 관리 동사가 **다른 낱말의 일부**일 때 밴·청소 명령으로 오인하던 문제. 이제 낱말 경계일 때만 관리로 인식합니다.

## 2026-07-14

- `추가` `/debug`: 현재 시각(KST)·핑·RAM·디스크·CPU 정보·네트워크 속도 실측·채널 수를 보여줍니다. 누구나 쓸 수 있고 동일인 1분 쿨타임.
- `변경` 소개 페이지와 README를 명령어 나열식으로 재구성했습니다. 명령이 늘수록 줄글 구성이 읽기 어려워져서입니다.

## 2026-07-13

- `변경` 4중 게이트를 **모두 통과한 성인 채널**에서는 명시적 표현이 없는 성인 창작 요청도 성인 모드로 처리합니다. 게이트가 하나라도 막힌 곳의 동작은 그대로입니다(누출 0).
- `수정` 연령 인증과 서버 설정은 됐는데 **채널 연령제한만 빠진** 경우, 이유 없이 거절하던 것을 무엇이 필요한지 안내하도록 바꿨습니다.

## 2026-07-12

- `추가` **점검 모드**: 점검 중에는 봇이 켜진 채로 모든 요청에 안내를 돌려줍니다(재시작해도 유지).
- `추가` `/reset` 대화 맥락 초기화, `/config status`·`/config reset`.
- `추가` `/untimeout`·`/unban` 해제 계열 관리 명령.
- `추가` 운영자용 관측 명령 `/cost`·`/audit`·`/health`.
- `수정` 관리·경고 명령이 3초 만료로 실패하던 문제, 그리고 **실제로는 성공했는데 실패로 보고**하던 오보.
- `수정` 투표의 '자동 마감(분)' 옵션이 실제로는 마감되지 않던 문제.
- `보안` 반응 역할을 **적용 시점에도** 재검증합니다. 설정한 뒤 역할 권한이 바뀌어 권한 상승 통로가 생기는 것을 막습니다.
- `변경` 24/7 운영 대비: 응답 없는 AI·OCR·다운로드 자동 회수, 종료 신호에 안전하게 마무리.

## 2026-07-10

- `보안` **봇 경유 멘션 남용 차단**. 봇에게 다른 유저나 `@everyone` 을 부르게 하는 요청은 스탭 이상만 가능하고, 봇 출력의 `@everyone`·`@here`·역할 핑은 항상 차단합니다.
- `수정` 중복 실행된 프로세스 때문에 한 질문에 두 번 답하던 문제(단일 인스턴스 잠금).
- `추가` `/pardon count`: 최근 N건만 사면해 경고 개수를 줄입니다.
- `추가` `/config log-channel`: 자동 모더레이션·경고·사면을 지정 채널에 기록합니다.
- `수정` 반말 모드인데 "네", "알겠습니다" 같은 존댓말이 섞이던 문제.
- `수정` 본문에 이미 있는 링크가 출처 각주에 중복으로 붙던 문제.

## 2026-07-09

- `변경` **요금제 기준을 대화 횟수에서 일일 토큰으로** 바꾸고 중간 등급(Basic)을 넣었습니다. 짧은 대화와 긴 코드 요청의 실제 비용 차이를 횟수로는 반영할 수 없었기 때문입니다.
- `변경` 결제를 원화(KRW) 표기로 바꿨습니다. 봇은 카드·계좌 정보를 받지 않습니다.
- `변경` 24/7 안정성 작업: xAI 호출 동시성 제한, OCR 대기열 상한, 서버 설정 캐시, 메모리 상한.

## 2026-07-08

- `추가` **이용약관·개인정보 처리방침** 공개.

## 2026-07-07

- `추가` 환영·작별 메시지(`/welcome`), 자동 역할(`/autorole`), 반응 역할(`/reactionrole`).
- `추가` 경고 시스템(`/warn`·`/warnings`·`/pardon`, 누적 시 자동 타임아웃).
- `추가` 투표·설문(`/poll`, 버튼 집계).
- `추가` 임시 음성채널(`/voice-hub` 허브 입장 시 개인 방 자동 생성) + 방장 제어 패널(`/voice`).
- `추가` 채널 잠금·해제(`/lock`·`/unlock`).

## 2026-07-03

- `추가` 이미지 번역(`/translate`). **이미지를 외부로 보내지 않고** 봇 안에서 글자만 뽑아 번역합니다.
- `추가` 채널 생성·삭제(`/create-channel`·`/delete-channel`).
- `보안` 보안 감사에서 나온 9건을 수정했습니다. 역할 부여 시 위계 검증, 미성년 하드차단 필터 보강, 여러 턴에 걸친 우회 시도 차단, 시스템 프롬프트 누출 재검증, 정규식 성능 공격(ReDoS) 차단.

## 2026-06-16 ~ 06-20

- `추가` URL 읽기(`/fetch`, 자연어 "이거 읽어줘"). 사설 IP·리다이렉트·크기·시간 상한 등 가드를 겁니다.
- `추가` GitHub 조회(`/repo`·`/gh-user`·`/gh-file`).
- `추가` 요금제·사용량 확인(`/usage`).
- `변경` **밴·킥·타임아웃·청소는 자연어로 실행되지 않습니다.** 슬래시 명령 + 확인 버튼 전용으로 못 박았습니다. 오인식 한 번의 대가가 너무 크기 때문입니다.
- `변경` 출처는 기본 3개까지 답니다(개수를 지정하면 그 수치).
- `보안` 미성년 관련 하드차단의 우회 표현(영문·은어·학년·나이 약어·문자 변형)을 대폭 보강했습니다.

## 2026-06-13 ~ 06-17

첫 공개 기능 세트입니다.

- `추가` Grok 기반 대화(반말 친근체), 발화 단위 모드 판정(일반·검색·코딩·서버 관리·성인).
- `추가` 호출 축약어 `그록아`(한국어)·`Hey Grok`(영어), 멘션, DM.
- `추가` 웹·X 실시간 검색 + 출처 표기.
- `추가` 이미지·파일 분석(SFW).
- `추가` 자연어 서버 관리(권한자 한정), 자동 모더레이션, 레벨링·리더보드, 초대 시 온보딩.
- `보안` **인가 단일 게이트**: 모든 관리 액션은 봇이 아니라 요청한 사람의 권한으로 판정하며, 슬래시와 자연어가 같은 게이트를 지납니다.
- `보안` **성인 모드 4중 게이트**(연령제한 채널 + 서버 활성 + 연령 인증 + 성적 의도)와 미성년 콘텐츠 하드차단.
- `추가` 유저별 대화 기억 분리(채널×사용자). 같은 채널이라도 다른 사람의 맥락이 섞이지 않습니다.

---

문의·제보: [jtech-bryan@proton.me](mailto:jtech-bryan@proton.me) · 보안 취약점은 [SECURITY.md](SECURITY.md) 참고.

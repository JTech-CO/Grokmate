# 변경 이력 (Changelog)

Grokmate에 반영된 변경을 이용자 관점에서 날짜순으로 정리.

> 웹에서 보기: [changelog.html](<https://jtech-co.github.io/Grokmate/changelog.html>) (같은 내용)

Grokmate는 **호스팅형 봇**이라 이용자가 버전을 고르지 않음. 판올림 번호 대신 **반영 날짜** 기준. 서버에 봇이 있으면 항상 최신 버전.

내부 리팩터링·테스트·문서 정리처럼 사용감이 그대로인 변경은 생략. 날짜별로 주요 항목 5건까지 펼쳐 두고 나머지는 접어 둠.

**분류**: `추가` 새 기능 · `변경` 동작·정책 변화 · `수정` 버그 · `보안` 권한·안전

---

## 2026-08-08

- `변경` **요금제 개편**: 일일 토큰 한도를 낮추고 상위 티어 가격을 올림. 한도가 실제 AI 사용료와 직결돼, 종전 한도로는 헤비 사용자가 몰릴수록 상위 티어일수록 손실이 커지는 구조였음
  - Free ₩0 · 하루 5만 → **3만** 토큰
  - Basic ₩2,900 · 15만 → **6만**
  - Pro ₩4,900 → **₩5,900** · 50만 → **15만**
  - Max ₩14,900 → **₩19,900** · 200만 → **70만**
  - 모든 기능은 요금제와 무관하게 그대로 열려 있음. 유료는 토큰 한도만 넓힘
- `변경` [약관 §9](legal/terms.html)에 요금제·한도가 운영 비용에 따라 변경될 수 있고 변경 시 이 변경 이력에 반영한다는 점을 명시

## 2026-08-06

- `추가` **`/config` 대화형 패널**: 서브커맨드 23개 → 버튼·셀렉트. 섹션 4종(기본·채널·역할·TTS), 채널·역할은 멀티 셀렉트로 한 번에 교체
- `보안` **봇 제거 시 해당 서버 데이터 삭제**: 설정·레벨·경고·커스텀 명령어. 권한 감사 기록은 보존(추방으로 증거 인멸 차단), 디스코드 장애 시에는 미삭제
- `추가` **`/guilds`** (운영자): 소속 서버·멤버·채널 목록 대시보드. 채널별 연령제한 표시, 서버 성인 모드까지 켜진 경우만 '게이트 열림'으로 구분
- `변경` **가동률 측정 시작**: 24시간·7일·30일 + 재시작 횟수. 기록에 이용자·서버 식별자 없음, 35일 후 삭제. 현재 운영자 전용(`/health`), 공개 상태 페이지는 데이터 축적 후
- `변경` **개인정보 삭제 요청 시 보존 항목 명시**([방침 §8](legal/privacy.html)): 감사 기록·경고·구독·커스텀 명령어는 남는다는 점과 이유를 표로 공개

<details>
<summary>나머지 4건 보기</summary>

- `변경` `/health` 가독성 개선: 값이 같은 창 접기, 부하 지표 한 줄 정리
- `변경` 약관 §11에 가동률 수치는 참고용 관측값이며 SLA 아님을 명시
- `수정` 가동률 기록의 35일 보관 정리가 재시작 때만 동작하던 문제
- `수정` 매 기동 시 "슬래시 명령 등록 실패" 오보(실제로는 성공, 개발용 부가 등록만 실패)

</details>

## 2026-08-05

- `추가` **커스텀 명령어·자동응답**(`/custom`): `!트리거` + 키워드 자동응답, AI 미경유로 토큰 소모 없음. 등록은 스탭 이상, 계정당 하루 5개·서버당 50개, `list`에서 바로 삭제·수정
- `추가` `/help` 권한별 두 뷰(일반 사용자 / 관리자) 버튼 전환
- `수정` 옛 문구 탓에 봇이 "실시간 검색 불가"로 잘못 답하던 문제
- `변경` 한국어 안내 문구의 영문식 대시(—)를 쌍점 등으로 교체

## 2026-08-04

- `추가` **음성 낭독(TTS)**: `/tts` 낭독 · `/tts-stop` 중지 · `/tts-settings` 개인 목소리(16종, 유지)·속도(0.8~1.5배, 음성방 퇴장 시 초기화). 지정 채널 자동 낭독(`/config tts-channel`), 사용자 입장 시 자동 입·퇴장. 링크·멘션·코드는 미낭독, **AI 답변은 음성 미출력**(음성 채널에 연령 게이트가 없어서)

## 2026-07-23

- `수정` '밴드'·'청소년'·'킥보드'처럼 관리 동사가 낱말 일부일 때 명령으로 오인하던 문제. 낱말 경계일 때만 인식

## 2026-07-14

- `추가` `/debug`: 시각(KST)·핑·RAM·디스크·CPU·네트워크 속도 실측·채널 수. 누구나 사용, 1분 쿨타임
- `변경` 소개 페이지·README를 명령어 나열식으로 재구성

## 2026-07-13

- `변경` 4중 게이트를 모두 통과한 성인 채널에서는 명시적 표현 없는 성인 창작 요청도 성인 모드로 처리. 게이트가 막힌 곳은 종전대로(누출 0)
- `수정` 연령 인증·서버 설정은 됐는데 채널 연령제한만 빠진 경우, 이유 없는 거절 대신 필요한 조치 안내

## 2026-07-12

- `추가` **점검 모드**: 점검 중 모든 요청에 안내(재시작해도 유지)
- `추가` 운영자 관측 명령 `/cost`·`/audit`·`/health`
- `추가` `/reset` 대화 맥락 초기화, `/config status`·`/config reset`
- `보안` 반응 역할을 **적용 시점에도** 재검증: 설정 후 역할 권한이 바뀌어 생기는 권한 상승 통로 차단
- `수정` 관리·경고 명령의 3초 만료 실패, 그리고 **성공했는데 실패로 보고**하던 오보

<details>
<summary>나머지 3건 보기</summary>

- `추가` `/untimeout`·`/unban` 해제 계열 관리 명령
- `수정` 투표의 '자동 마감(분)'이 실제로 마감되지 않던 문제
- `변경` 24/7 대비: 응답 없는 AI·OCR·다운로드 자동 회수, 종료 신호에 안전 마무리

</details>

## 2026-07-10

- `보안` **봇 경유 멘션 남용 차단**: 봇에게 타 유저·`@everyone` 호출을 시키는 요청은 스탭 이상만. 봇 출력의 `@everyone`·`@here`·역할 핑은 항상 차단
- `수정` 중복 프로세스로 한 질문에 두 번 답하던 문제(단일 인스턴스 잠금)
- `추가` `/config log-channel`: 자동 모더레이션·경고·사면을 지정 채널에 기록
- `추가` `/pardon count`: 최근 N건만 사면
- `수정` 반말 모드에 존댓말이 섞이던 문제

<details>
<summary>나머지 1건 보기</summary>

- `수정` 본문에 이미 있는 링크가 출처 각주에 중복으로 붙던 문제

</details>

## 2026-07-09

- `변경` **요금제 기준을 대화 횟수에서 일일 토큰으로** 전환 + 중간 등급(Basic) 신설. 짧은 대화와 긴 코드 요청의 비용 차이를 횟수로는 반영 불가
- `변경` 결제를 원화(KRW) 표기로 전환. 카드·계좌 정보는 받지 않음
- `변경` 24/7 안정성: xAI 호출 동시성 제한, OCR 대기열 상한, 설정 캐시, 메모리 상한

## 2026-07-08

- `추가` **[이용약관](legal/terms.html)·[개인정보 처리방침](legal/privacy.html)** 공개

## 2026-07-07

- `추가` 환영·작별 메시지(`/welcome`), 자동 역할(`/autorole`), 반응 역할(`/reactionrole`)
- `추가` 경고 시스템(`/warn`·`/warnings`·`/pardon`), 누적 시 자동 타임아웃
- `추가` 투표·설문(`/poll`, 버튼 집계)
- `추가` 임시 음성채널(`/voice-hub` 허브 입장 시 개인 방 자동 생성) + 방장 제어 패널(`/voice`)
- `추가` 채널 잠금·해제(`/lock`·`/unlock`)

## 2026-07-03

- `추가` 이미지 번역(`/translate`): **이미지를 외부로 보내지 않고** 봇 내부에서 글자만 추출해 번역
- `추가` 채널 생성·삭제(`/create-channel`·`/delete-channel`)
- `보안` 보안 감사 9건 수정: 역할 위계 검증, 미성년 하드차단 보강, 멀티턴 우회 차단, 시스템 프롬프트 누출 재검증, 정규식 성능 공격(ReDoS) 차단

## 2026-06-16 ~ 06-20

- `변경` **밴·킥·타임아웃·청소는 자연어로 실행 불가**. 슬래시 + 확인 버튼 전용. 오인식 한 번의 대가가 너무 큼
- `보안` 미성년 하드차단의 우회 표현(영문·은어·학년·나이 약어·문자 변형) 대폭 보강
- `추가` URL 읽기(`/fetch`, 자연어 "이거 읽어줘"): 사설 IP·리다이렉트·크기·시간 상한 가드
- `추가` GitHub 조회(`/repo`·`/gh-user`·`/gh-file`)
- `추가` 요금제·사용량 확인(`/usage`)

<details>
<summary>나머지 1건 보기</summary>

- `변경` 출처는 기본 3개까지 표기(개수 지정 시 그 수치)

</details>

## 2026-06-13 ~ 06-17

첫 공개 기능 세트.

- `보안` **인가 단일 게이트**: 모든 관리 액션을 봇이 아니라 요청자 권한으로 판정. 슬래시와 자연어가 같은 게이트 경유
- `보안` **성인 모드 4중 게이트**(연령제한 채널 + 서버 활성 + 연령 인증 + 성적 의도) + 미성년 콘텐츠 하드차단
- `추가` Grok 기반 대화(반말 친근체), 발화 단위 모드 판정(일반·검색·코딩·서버 관리·성인)
- `추가` 호출 축약어 `그록아`(한국어)·`Hey Grok`(영어), 멘션, DM
- `추가` 웹·X 실시간 검색 + 출처 표기

<details>
<summary>나머지 3건 보기</summary>

- `추가` 이미지·파일 분석(SFW)
- `추가` 자연어 서버 관리(권한자 한정), 자동 모더레이션, 레벨링·리더보드, 초대 시 온보딩
- `추가` 유저별 대화 기억 분리(채널×사용자). 같은 채널이라도 타인의 맥락이 섞이지 않음

</details>

---

문의·제보: [jtech-bryan@proton.me](mailto:jtech-bryan@proton.me)

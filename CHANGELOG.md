# 변경 이력 (Changelog)

Grokmate에 반영된 변경을 이용자 관점에서 날짜순으로 정리.

> 웹에서 보기: [changelog.html](<https://jtech-co.github.io/Grokmate/changelog.html>) (같은 내용)

Grokmate는 **호스팅형 봇**이라 이용자가 버전을 고르지 않음. 판올림 번호 대신 **반영 날짜** 기준. 서버에 봇이 있으면 항상 최신 버전.

내부 리팩터링·테스트·문서 정리처럼 사용감이 그대로인 변경은 생략. 날짜별로 주요 항목 5건까지 펼쳐 두고 나머지는 접어 둠.

**분류**: `추가` 새 기능 · `변경` 동작·정책 변화 · `수정` 버그 · `보안` 권한·안전

---

## 2026-08-19

- `추가` **긴 대화의 앞부분을 기억합니다**: 예전에는 최근 20개 메시지를 넘어가면 앞 내용이 그냥 사라졌고, 봇을 재시작하면 모든 대화가 백지가 됐습니다. 이제 밀려난 앞부분을 짧은 요약으로 압축해 두고 이후 대화에 참고합니다
  - **재시작해도 이어집니다.** 이름·선호·진행 중인 작업처럼 앞에서 말한 것을 계속 기억합니다
  - 요약이지 원문이 아니라서 세부는 빠질 수 있습니다. 봇도 그 사실을 알고 있어서 확실하지 않으면 되묻습니다
  - `/reset` 을 쓰면 최근 대화창과 **저장된 요약을 함께** 지웁니다. "잊었다"고 해놓고 기억하는 일은 없습니다
  - 한 번에 보내는 대화량에 글자 상한이 생겨, 긴 글을 여러 번 붙여넣어도 대화가 무거워지지 않습니다
- `보안` 요약은 **성적인 내용이 포함된 대화에는 만들지 않습니다.** 요약은 이후 모든 대화에 참고 자료로 들어가는데, 성인 맥락이 거기 남으면 일반 대화에 새어 나올 수 있기 때문입니다. 요약에 담긴 문장은 봇에게 **지시가 아니라 참고 자료**로만 전달됩니다
- `변경` [개인정보 처리방침](legal/privacy.html)에 **대화 요약** 항목을 추가하고, "대화 맥락은 재시작 시 소멸" 문구를 실제 동작에 맞게 고쳤습니다(원문은 소멸, 요약은 유지·`/reset` 으로 삭제)

- `추가` **서버별 운영 대시보드** (`/guilds`, 운영자): 서버를 골라 그 서버만 **일시정지/재개**하거나, **런타임 초기화**(대화 맥락 + 설정 캐시), **대화 맥락만 초기화**, **성인 모드 끄기**, **일일 토큰 상한**을 조작할 수 있습니다
  - 전에는 한 서버에서 문제가 생기면 봇 전체를 재시작해야 했고, 그동안 **다른 모든 서버가 함께 멈췄습니다.** 이제 문제가 생긴 서버만 조치하면 되고, 재시작 대기 시간도 없습니다
  - 일시정지 중인 서버에서는 봇이 대화·명령에 응답하지 않고 그 사실을 안내합니다(같은 채널에는 10분에 한 번만 안내해 안내가 도배되지 않게 합니다). **다른 서버는 아무 영향이 없습니다**
  - 서버별 토큰 사용량을 집계하고, 일일 상한을 넘으면 **그 서버의 AI 응답만** 막힙니다. 개인 이용 한도와는 별개로 함께 적용됩니다
  - 성인 모드는 **끄기만** 할 수 있습니다. 다시 켜는 것은 그 서버 관리자가 `/nsfw-policy` 로 직접 해야 합니다 — 서버 관리자 동의 없이 성인 경로가 열리는 일을 원천 차단합니다
  - 모든 조치는 누가·언제·어느 서버에 했는지 감사 로그(`/audit`)에 남습니다
- `변경` [개인정보 처리방침](legal/privacy.html)에 **서버 운영 상태**(일시정지 여부·사유·시각, 일일 토큰 상한) 항목을 추가했습니다
- `추가` **허깅페이스 조회**: `/hf-repo` 모델·데이터셋·스페이스 검색 · `/hf-user` 유저·조직의 레포 목록 · `/hf-file` 파일 내용·디렉터리 개요. GitHub·GitLab 명령과 짝이 맞습니다
  - **정렬**을 고를 수 있습니다: 다운로드순·좋아요순·최근 수정순·최근 생성순
  - `gpt2`·`bert-base-uncased` 같은 **한 낱말짜리 이름**도 그대로 열립니다(정식 이름으로 자동 연결). 웹 주소 붙여넣기, 브랜치 지정도 됩니다
  - 허깅페이스 검색은 레포 **이름** 기준이라 낱말을 여러 개 넣으면 결과가 급격히 줄어듭니다. 0건이면 낱말을 하나씩 줄여 다시 찾고, **줄여서 찾았다는 사실을 함께 알려줍니다**
  - 한국어 검색어는 결과가 거의 없습니다(레포 이름이 대부분 영문). 한국어로 쳤는데 0건이면 영문 키워드를 권합니다
  - 게이트된 레포(제작자 승인이 필요한 것)는 "없다"가 아니라 **승인이 필요하다**고 정확히 알려줍니다
- `보안` 검색 결과에 섞여 들어오는 성인·무검열 레포를 일반 채널에서 제외합니다. "character"·"story" 같은 평범한 검색어에도 섞여 나오는 것을 실측으로 확인했습니다. 제외한 경우 **몇 건을 뺐는지 함께 표시**하며, 연령제한 채널에서는 그대로 보입니다
- `변경` [개인정보 처리방침 §4](legal/privacy.html)에 허깅페이스를 추가했습니다. 조회 요청(검색어·경로)만 전송되며 이용자·서버 식별자는 함께 보내지 않습니다

## 2026-08-18

- `추가` **GitLab 조회**: `/gl-repo` 프로젝트 검색 · `/gl-user` 유저·그룹의 프로젝트 목록 · `/gl-file` 파일 내용·디렉터리 개요. 기존 GitHub 명령과 짝이 맞습니다
  - **중첩 그룹**을 지원합니다(`group/subgroup/project`). 웹 주소를 그대로 붙여넣어도 되고, 브랜치를 지정할 수도 있습니다
  - `/gl-user` 는 유저와 그룹을 모두 찾고 어느 쪽인지 표시합니다. 그룹은 하위 그룹의 프로젝트까지 함께 보여줍니다
  - GitLab 검색은 **3글자 이상**이어야 결과가 나옵니다. 짧으면 정확히 일치하는 것만 찾아서 "게임"·"번역" 같은 두 글자 한국어는 결과가 비는데, 이제 그렇게 안내합니다
- `추가` GitHub 검색을 **`/gh-repo`** 로도 부를 수 있습니다. 기존 `/repo` 는 그대로 동작하며, 이제 슬래시 목록에서 `gh-` 를 치면 GitHub 3종이 나란히 뜹니다(GitLab 쪽과 짝이 맞습니다)
- `보안` GitLab 프로젝트 이름·설명은 누구나 정하는 값이라, 표시 전에 정제합니다. 링크처럼 보이게 만드는 표기를 무력화하고(가짜 "공식 다운로드" 방지), 디스코드 초대 링크는 끊고, 보이지 않는 문자는 제거합니다
- `변경` [개인정보 처리방침 §4](legal/privacy.html)에 GitLab 을 추가했습니다. 조회 요청(검색어·경로)만 전송되며 이용자·서버 식별자는 함께 보내지 않습니다

## 2026-08-14

- `추가` **`/hn` Hacker News 검색**: 키워드로 글을 찾고 **인기순·최신순·댓글순**으로 정렬, 기간은 24시간·1주·1개월·1년·전체 중 선택, 개수도 지정. 결과에 원문 링크와 HN 토론 링크를 함께 표시
  - **말로 시켜도** 됩니다(`해커뉴스`·`Hacker News`·`HN` 모두 인식). "해커뉴스에서 Machine Learning 다룬 최신 기사 3개만 찾아줘" 처럼 하면 **정렬·기간·개수까지** 알아듣습니다("3개만"·"세 개"·"top 5", `last week`·`yesterday`)
  - **HN을 화제로 삼은 말은 검색으로 치지 않습니다.** "Hacker News가 뭐 하는 사이트야?", "HN 소개 글 써줘", "해커뉴스에 글 올리는 법" 처럼 HN에 **대해** 묻거나 글을 써 달라는 말은 평소처럼 AI가 답합니다. "해커뉴스**에서** …" 처럼 HN을 출처로 가리킬 때만 검색합니다
  - **AI를 거치지 않아 토큰을 쓰지 않음** — 일일 한도가 줄지 않습니다. 커스텀 명령어와 같은 취급
  - HN은 영어권 사이트라 영어 키워드가 잘 맞습니다. 한국어가 섞여 결과가 없으면 **영어 부분으로 한 번 더 찾고** 그렇게 했다고 밝힙니다(처음부터 영어만 쓰면 "의료 AI"를 "AI"로 바꿔 찾는 셈이라 묻지 않은 걸 답하게 됩니다)
- `보안` HN 글 제목·링크는 외부 사용자가 올린 것이라, 표시 전에 안전 필터를 거치고 성인 관련 항목은 연령제한 채널에서만 보여줍니다(제외한 건수는 밝힘). 제목에 `@everyone` 이 있어도 알림이 가지 않음
- `변경` 링크를 디스코드·카카오톡에 공유하면 [소개 페이지](<https://jtech-co.github.io/Grokmate/>) 미리보기 카드가 제대로 뜨도록 전용 이미지 추가(종전에는 작은 정사각 로고)

## 2026-08-11

- `변경` **일일 토큰 한도 초기화 시각을 한국시간 자정으로** 변경(종전 UTC 자정 = 한국시간 오전 9시). 밤에 한도를 다 쓰면 다음 날 아침 9시까지 기다려야 했던 문제 해소
  - `/tts` 문자 한도도 같은 시각에 초기화됩니다
  - [약관 §9](legal/terms.html) 문구도 함께 정정

## 2026-08-08

- `수정` **성인 모드가 조건을 다 갖췄는데도 안 열리던 문제**. 관리자가 채널 연령제한을 켜도 봇이 그 변경을 놓치면 재시작 전까지 계속 막혔음. 이제 다른 조건이 모두 충족됐는데 채널만 막혀 있으면 디스코드에 한 번 더 확인함
- `추가` **성인 모드 여는 법 안내**: 서버에 봇을 추가하면 환영 메시지에, `/nsfw-policy` 로 켜면 그 결과에 남은 단계를 함께 표시
  - 특히 **`/verify-age` 는 대화할 사람이 각자 한 번씩** 눌러야 한다는 점을 명시. 디스코드 자체 연령 확인과 별개라 그 결과가 봇에 전달되지 않음. 여기서 가장 많이 막힘
- `변경` **요금제 개편**: 일일 토큰 한도를 낮추고 상위 티어 가격을 올림. 한도가 실제 AI 사용료와 직결돼, 종전 한도로는 헤비 사용자가 몰릴수록 상위 티어일수록 손실이 커지는 구조였음
  - Free ₩0 · 하루 5만 → **3만** 토큰
  - Basic ₩2,900 · 15만 → **6만**
  - Pro ₩4,900 → **₩5,900** · 50만 → **15만**
  - Max ₩14,900 → **₩19,900** · 200만 → **70만**
  - 모든 기능은 요금제와 무관하게 그대로 열려 있음. 유료는 토큰 한도만 넓힘
- `변경` [약관 §9](legal/terms.html)에 요금제·한도가 운영 비용에 따라 변경될 수 있고 변경 시 이 변경 이력에 반영한다는 점을 명시

## 2026-08-06

- `추가` **`/config` 대화형 패널**: 서브커맨드 23개 → 버튼·셀렉트. 섹션 4종(기본·채널·역할·TTS), 채널·역할은 멀티 셀렉트로 한 번에 교체
- `보안` **봇 제거 시 해당 서버 데이터 삭제**: 설정·레벨·경고·커스텀 명령어. 권한 감사 기록은 보존(추방으로 증거 인멸 차단), 디스코드 장애 시에는 미삭제
- `추가` **`/guilds`** (운영자): 소속 서버·멤버·채널 목록 대시보드. 채널별 연령제한 표시, 서버 성인 모드까지 켜진 경우만 '게이트 열림'으로 구분
- `변경` **가동률 측정 시작**: 24시간·7일·30일 + 재시작 횟수. 기록에 이용자·서버 식별자 없음, 35일 후 삭제. 현재 운영자 전용(`/health`), 공개 상태 페이지는 데이터 축적 후
- `변경` **개인정보 삭제 요청 시 보존 항목 명시**([방침 §8](legal/privacy.html)): 감사 기록·경고·구독·커스텀 명령어는 남는다는 점과 이유를 표로 공개

<details>
<summary>나머지 4건 보기</summary>

- `변경` `/health` 가독성 개선: 값이 같은 창 접기, 부하 지표 한 줄 정리
- `변경` 약관 §11에 가동률 수치는 참고용 관측값이며 SLA 아님을 명시
- `수정` 가동률 기록의 35일 보관 정리가 재시작 때만 동작하던 문제
- `수정` 매 기동 시 "슬래시 명령 등록 실패" 오보(실제로는 성공, 개발용 부가 등록만 실패)

</details>

## 2026-08-05

- `추가` **커스텀 명령어·자동응답**(`/custom`): `!트리거` + 키워드 자동응답, AI 미경유로 토큰 소모 없음. 등록은 스탭 이상, 계정당 하루 5개·서버당 50개, `list`에서 바로 삭제·수정
- `추가` `/help` 권한별 두 뷰(일반 사용자 / 관리자) 버튼 전환
- `수정` 옛 문구 탓에 봇이 "실시간 검색 불가"로 잘못 답하던 문제
- `변경` 한국어 안내 문구의 영문식 대시(—)를 쌍점 등으로 교체

## 2026-08-04

- `추가` **음성 낭독(TTS)**: `/tts` 낭독 · `/tts-stop` 중지 · `/tts-settings` 개인 목소리(16종, 유지)·속도(0.8~1.5배, 음성방 퇴장 시 초기화). 지정 채널 자동 낭독(`/config tts-channel`), 사용자 입장 시 자동 입·퇴장. 링크·멘션·코드는 미낭독, **AI 답변은 음성 미출력**(음성 채널에 연령 게이트가 없어서)

## 2026-07-23

- `수정` '밴드'·'청소년'·'킥보드'처럼 관리 동사가 낱말 일부일 때 명령으로 오인하던 문제. 낱말 경계일 때만 인식

## 2026-07-14

- `추가` `/debug`: 시각(KST)·핑·RAM·디스크·CPU·네트워크 속도 실측·채널 수. 누구나 사용, 1분 쿨타임
- `변경` 소개 페이지·README를 명령어 나열식으로 재구성

## 2026-07-13

- `변경` 4중 게이트를 모두 통과한 성인 채널에서는 명시적 표현 없는 성인 창작 요청도 성인 모드로 처리. 게이트가 막힌 곳은 종전대로(누출 0)
- `수정` 연령 인증·서버 설정은 됐는데 채널 연령제한만 빠진 경우, 이유 없는 거절 대신 필요한 조치 안내

## 2026-07-12

- `추가` **점검 모드**: 점검 중 모든 요청에 안내(재시작해도 유지)
- `추가` 운영자 관측 명령 `/cost`·`/audit`·`/health`
- `추가` `/reset` 대화 맥락 초기화, `/config status`·`/config reset`
- `보안` 반응 역할을 **적용 시점에도** 재검증: 설정 후 역할 권한이 바뀌어 생기는 권한 상승 통로 차단
- `수정` 관리·경고 명령의 3초 만료 실패, 그리고 **성공했는데 실패로 보고**하던 오보

<details>
<summary>나머지 3건 보기</summary>

- `추가` `/untimeout`·`/unban` 해제 계열 관리 명령
- `수정` 투표의 '자동 마감(분)'이 실제로 마감되지 않던 문제
- `변경` 24/7 대비: 응답 없는 AI·OCR·다운로드 자동 회수, 종료 신호에 안전 마무리

</details>

## 2026-07-10

- `보안` **봇 경유 멘션 남용 차단**: 봇에게 타 유저·`@everyone` 호출을 시키는 요청은 스탭 이상만. 봇 출력의 `@everyone`·`@here`·역할 핑은 항상 차단
- `수정` 중복 프로세스로 한 질문에 두 번 답하던 문제(단일 인스턴스 잠금)
- `추가` `/config log-channel`: 자동 모더레이션·경고·사면을 지정 채널에 기록
- `추가` `/pardon count`: 최근 N건만 사면
- `수정` 반말 모드에 존댓말이 섞이던 문제

<details>
<summary>나머지 1건 보기</summary>

- `수정` 본문에 이미 있는 링크가 출처 각주에 중복으로 붙던 문제

</details>

## 2026-07-09

- `변경` **요금제 기준을 대화 횟수에서 일일 토큰으로** 전환 + 중간 등급(Basic) 신설. 짧은 대화와 긴 코드 요청의 비용 차이를 횟수로는 반영 불가
- `변경` 결제를 원화(KRW) 표기로 전환. 카드·계좌 정보는 받지 않음
- `변경` 24/7 안정성: xAI 호출 동시성 제한, OCR 대기열 상한, 설정 캐시, 메모리 상한

## 2026-07-08

- `추가` **[이용약관](legal/terms.html)·[개인정보 처리방침](legal/privacy.html)** 공개

## 2026-07-07

- `추가` 환영·작별 메시지(`/welcome`), 자동 역할(`/autorole`), 반응 역할(`/reactionrole`)
- `추가` 경고 시스템(`/warn`·`/warnings`·`/pardon`), 누적 시 자동 타임아웃
- `추가` 투표·설문(`/poll`, 버튼 집계)
- `추가` 임시 음성채널(`/voice-hub` 허브 입장 시 개인 방 자동 생성) + 방장 제어 패널(`/voice`)
- `추가` 채널 잠금·해제(`/lock`·`/unlock`)

## 2026-07-03

- `추가` 이미지 번역(`/translate`): **이미지를 외부로 보내지 않고** 봇 내부에서 글자만 추출해 번역
- `추가` 채널 생성·삭제(`/create-channel`·`/delete-channel`)
- `보안` 보안 감사 9건 수정: 역할 위계 검증, 미성년 하드차단 보강, 멀티턴 우회 차단, 시스템 프롬프트 누출 재검증, 정규식 성능 공격(ReDoS) 차단

## 2026-06-16 ~ 06-20

- `변경` **밴·킥·타임아웃·청소는 자연어로 실행 불가**. 슬래시 + 확인 버튼 전용. 오인식 한 번의 대가가 너무 큼
- `보안` 미성년 하드차단의 우회 표현(영문·은어·학년·나이 약어·문자 변형) 대폭 보강
- `추가` URL 읽기(`/fetch`, 자연어 "이거 읽어줘"): 사설 IP·리다이렉트·크기·시간 상한 가드
- `추가` GitHub 조회(`/repo`·`/gh-user`·`/gh-file`)
- `추가` 요금제·사용량 확인(`/usage`)

<details>
<summary>나머지 1건 보기</summary>

- `변경` 출처는 기본 3개까지 표기(개수 지정 시 그 수치)

</details>

## 2026-06-13 ~ 06-17

첫 공개 기능 세트.

- `보안` **인가 단일 게이트**: 모든 관리 액션을 봇이 아니라 요청자 권한으로 판정. 슬래시와 자연어가 같은 게이트 경유
- `보안` **성인 모드 4중 게이트**(연령제한 채널 + 서버 활성 + 연령 인증 + 성적 의도) + 미성년 콘텐츠 하드차단
- `추가` Grok 기반 대화(반말 친근체), 발화 단위 모드 판정(일반·검색·코딩·서버 관리·성인)
- `추가` 호출 축약어 `그록아`(한국어)·`Hey Grok`(영어), 멘션, DM
- `추가` 웹·X 실시간 검색 + 출처 표기

<details>
<summary>나머지 3건 보기</summary>

- `추가` 이미지·파일 분석(SFW)
- `추가` 자연어 서버 관리(권한자 한정), 자동 모더레이션, 레벨링·리더보드, 초대 시 온보딩
- `추가` 유저별 대화 기억 분리(채널×사용자). 같은 채널이라도 타인의 맥락이 섞이지 않음

</details>

---

문의·제보: [jtech-bryan@proton.me](mailto:jtech-bryan@proton.me)

# 개인정보 처리방침 (Privacy Policy)

**시행일: 2026-07-08 · 최종 개정: 2026-08-19**

> 정식 게시본(HTML): [`legal/privacy.html`](legal/privacy.html). 이 문서는 GitHub 열람용 요약이며, 내용이 다를 경우 HTML 게시본이 우선합니다. 함께 [이용약관](legal/terms.html)도 확인하세요.

Grokmate("봇")가 디스코드에서 사용자 데이터를 어떻게 처리하는지 설명합니다. 봇을 서버에 추가하거나 봇과 상호작용하면 본 방침에 따른 처리에 동의하는 것으로 간주됩니다.

## 1. 수집·처리하는 데이터

실명·생년월일·주소·전화번호·신분증 등 오프라인 신원 정보는 수집하지 않습니다.

- **디스코드 식별자**: 사용자·서버(길드)·채널·메시지·역할 ID. 봇 동작·권한 판정·기능 연결에 사용.
- **메시지·콘텐츠**: 봇을 호출(웨이크워드/멘션/DM)한 메시지, 첨부 이미지·텍스트/코드 파일, `/fetch`·`/translate` 입력. 응답·분석 생성과 단기 대화 맥락 유지에 사용.
- **연령 인증 상태**: `/verify-age` 사용 시 "만 19세 이상" 자가 인증 여부(불리언)·시각·방식. **생년월일·신분증은 수집하지 않습니다.**
- **서버 설정**: 성인 모드 활성, 자동 모더레이션, 관리·스탭 역할, 환영·작별·자동역할·임시음성 허브 등
- **서버 운영 상태**: 봇 운영자가 서버 단위로 설정하는 일시정지 여부·사유·시각과 일일 토큰 상한. 장애 대응 시 해당 서버만 격리하기 위해 사용합니다.
- **대화 요약**: 최근 창에서 밀려난 대화를 압축한 짧은 메모(채널×사용자 단위). 원문이 아니라 요약이며, 긴 대화의 앞 맥락을 이어가는 데만 사용합니다. `/reset` 으로 삭제됩니다.
- **서버 지식베이스 문서**: 서버 관리자가 `/kb` 로 직접 등록한 문서(규칙·공지·FAQ 등). **관리자가 넣은 것만** 들어가며, 일반 대화 내용이 자동으로 수집되지는 않습니다. 아래 §4 대로 검색 색인을 위해 xAI에 저장됩니다.
- **모더레이션·경고**: 자동 모더레이션 로그, 경고 기록(대상·발부자·사유·시각), 관리 감사 로그(행위자·액션·대상·결과·시각).
- **활동·레벨**: 서버·사용자별 누적 XP·레벨.
- **사용량·토큰**: 일일 토큰 사용량(입력/출력), 메시지 수, 서버 단위 사용량·비용 메터. 요금제 한도는 **일일 토큰** 기준.
- **결제(선택)**: 요금제(Free/Basic/Pro/Max)·상태·유효 기간 종료 시각. **결제는 봇 밖(계좌이체·간편결제)에서 이루어지며, 봇은 카드·계좌 등 결제수단 정보를 수집·저장하지 않습니다.**
- **기능별 데이터**: 반응 역할 매핑, 임시 음성채널 소유권, 투표 및 투표 표(선택 항목).
- **TTS(음성 낭독) 설정**: 사용자별 목소리 선택값. 재생 속도는 저장하지 않습니다(메모리에만 두며 음성 채널을 나가면 사라짐). **음성 자체는 저장하지 않고 재생 후 폐기합니다.**
- **커스텀 명령어·자동응답**: 서버가 등록한 트리거·응답 문구와 만든 사람의 사용자 ID·시각. 응답은 **서버 구성원이 직접 작성한 문구**이며, 삭제하면 함께 지워집니다. AI를 거치지 않으므로 이 문구는 xAI로 전송되지 않습니다.
- **가동률 기록**: 봇이 살아 있는 동안 1분마다 남기는 시각과 연결 상태(가동률 계산용). **이용자·서버 식별자를 포함하지 않으며** 35일 후 자동 삭제됩니다.

**대화 원문은 단기·인메모리로만 유지**됩니다(채널×사용자 단위, 최근 약 20개 메시지). 원문은 영구 저장하지 않으며 **봇 재시작 시 소멸**합니다.

다만 그 창을 넘어 밀려난 앞부분은 **짧은 요약으로 압축해 저장**합니다(채널×사용자 단위). 긴 대화에서 앞 맥락을 이어가기 위한 것으로, **원문이 아니라 요약**이며 재시작 후에도 유지됩니다. `/reset` 을 쓰면 대화 맥락과 요약이 **함께** 지워집니다. 성적인 내용이 포함된 대화는 **요약하지 않습니다**(만들지도, 저장하지도 않습니다).

## 2. 이용 목적

- 이용자 요청에 응답하고 봇 기능을 제공.
- 관리 액션을 요청자 권한으로만 실행하고 결과를 감사 기록.
- 성인 콘텐츠 게이팅·미성년 보호·안전 필터 적용.
- 요금제 한도 적용 및 결제 처리, 오남용 탐지.

위 목적 외로 데이터를 **판매·광고 타게팅·제3자 마케팅 공유하지 않습니다.**

## 3. 저장과 보관

- 대화 **원문**·일부 레이트리밋 상태는 인메모리·단기(재시작 시 소멸).
- 대화 **요약**은 데이터베이스에 저장되어 재시작 후에도 유지(`/reset`·서버 초기화 시 삭제).
- **서버 지식베이스 문서**는 검색 색인을 위해 **xAI에 저장**됩니다(원문 보관). `/kb remove`·`/kb clear` 로 지우면 xAI 쪽에서도 함께 삭제되며, 봇이 서버에서 제거되면 자동으로 정리됩니다.
- 서버 설정·연령 인증·레벨·경고·감사 로그·사용량·구독은 봇 운영용 데이터베이스에 저장.
- 봇이 서버에서 제거되거나 삭제 요청 시 처리·보관을 중단·삭제(감사·법적 의무상 최소 보관 항목 제외).

## 4. 제3자 처리

- **Discord**: 봇이 동작하는 플랫폼.
- **xAI (Grok API)**: 응답·분석 생성을 위해 메시지 본문·분석 이미지·검색 질의 등이 전송(xAI 정책 적용).
- **웹 / X 검색**: 검색 모드 사용 시 검색 질의 전송.
- **GitHub API**: `/repo`·`/gh-user`·`/gh-file` 사용 시.
- **GitLab API**: `/gl-repo`·`/gl-user`·`/gl-file` 사용 시 조회 요청(검색어·경로)만 전송(이용자·서버 식별자 미전송, xAI 미경유).
- **Hugging Face Hub API**: `/hf-repo`·`/hf-user`·`/hf-file` 사용 시 조회 요청(검색어·경로)만 전송(이용자·서버 식별자 미전송, xAI 미경유).
- **xAI (지식베이스)**: `/kb` 로 등록한 문서가 **xAI에 업로드·저장**되어 임베딩 색인됩니다. 대화 중 관련 질문이 오면 그 문서를 검색해 근거로 답합니다. 등록은 서버 관리자만 할 수 있고, 삭제하면 xAI 쪽도 함께 지워집니다.
- **Hacker News 검색 (Algolia)**: `/hn` 또는 자연어 HN 요청 시 **검색어**만 전송(이용자·서버 식별자 미전송, xAI 미경유).
- **사용자가 지정한 URL**: `/fetch`·자연어 링크 읽기 사용 시 해당 페이지를 가져옴.
- **Cloudflare**: `/debug` 네트워크 속도 측정 시 공개 speed 엔드포인트와 더미 데이터만 주고받음(이용자 데이터·개인정보 미전송).
- **xAI (TTS)**: 음성 낭독 시 **읽을 텍스트**가 음성 합성을 위해 xAI로 전송됩니다.

> **자동 낭독 고지 (중요).** 서버 관리자가 `/config tts-channel` 로 자동 낭독 채널을 지정하면, **그 채널의 메시지는 봇을 호출하지 않아도** 음성 합성을 위해 xAI로 전송됩니다(봇이 음성 채널에 있을 때). 링크·멘션·코드블록은 전송 전에 제거되며, 관리자는 이 사실을 서버 구성원에게 알려야 합니다. 원치 않으면 `/config tts-channel off` 로 끄거나 해당 채널을 지정하지 마세요.

유료 플랜 결제는 계좌이체·간편결제 등 봇 밖에서 이루어지며, 결제 데이터를 처리하는 제3자 결제대행사(PG)를 봇에 연동하고 있지 않습니다.

> **이미지 번역(`/translate`)은 이미지를 외부로 보내지 않습니다.** 봇 내부 OCR(WASM)로 글자만 추출한 뒤, 추출된 텍스트만 번역을 위해 xAI로 전송합니다.

## 5. 국외 이전

위 제3자(특히 xAI·GitHub·Discord)는 대한민국 외(예: 미국)에 서버를 둘 수 있으며, 기능 제공에 필요한 범위에서 데이터가 국외로 이전·처리될 수 있습니다.

## 6. 보안

- 시크릿(API 키·봇 토큰)은 코드·로그에 노출되지 않습니다.
- 관리 액션은 요청자 권한 단일 게이트 + 전수 감사.
- 외부 URL 가져오기는 사설/예약 IP·DNS 리바인딩 차단, robots.txt 확인, 레이트리밋, 크기·시간 상한 등 가드를 거칩니다.
- 결제 서명 검증, 요금제는 실제 결제 정보로만 판정.

## 7. 미성년자

- 성인 기능은 만 19세 이상 + 연령제한 채널 등 4중 게이트를 모두 통과해야만 동작.
- 미성년자 관련 성적 콘텐츠는 어떤 경우에도 차단.
- 디스코드 이용약관상 만 13세 미만은 디스코드를 사용할 수 없습니다.

## 8. 사용자 권리

- 본인 데이터의 열람·정정·삭제를 요청할 수 있습니다(아래 연락처).
- 서버에서 봇을 제거하면 해당 서버 관련 데이터 처리는 중단되고, 그 서버의 운영 데이터는 삭제됩니다.

**삭제 요청 시 지워지는 것**: 연령 인증 상태, 프로필 설정(TTS 목소리·언어), 서버별 레벨·XP, 일일 토큰 사용량, 투표에 던진 표.

**삭제 요청에도 남는 것과 이유** (요청 처리 시 함께 안내합니다):

| 항목 | 남기는 이유 |
|---|---|
| 관리 감사 로그 | 관리 액션의 책임 추적에 필요(§3 감사 목적 보관). 삭제 가능하면 권한 남용 후 요청으로 기록을 없앨 수 있습니다. |
| 경고·모더레이션 기록 | 해당 **서버의 운영 기록**입니다. 본인이 지울 수 있으면 경고가 무력화됩니다. 사면은 서버 관리자가 판단합니다. |
| 구독·결제 이력 | 유효한 이용 권리이자 거래 기록. 삭제하면 이미 지불한 이용권이 사라집니다. 해지는 별도 요청. |
| 등록한 커스텀 명령어 | 서버에서 동작 중인 콘텐츠(작성자 식별자만 포함). 삭제하면 서버 설정이 깨지므로 서버 관리자가 `/custom` 에서 처리합니다. |

## 9. 변경

본 방침은 변경될 수 있으며, 변경 시 상단 시행일·개정일을 갱신합니다.

## 10. 연락처

개인정보 관련 문의: **[jtech-bryan@proton.me](mailto:jtech-bryan@proton.me)**

---

Grokmate는 비공식 독립 프로젝트이며 xAI · X · Discord 와 제휴 관계가 없습니다.
